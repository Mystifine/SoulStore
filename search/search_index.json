{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"SoulStore","text":"<p>Version: 1.0.3</p> <p>SoulStore is a lightweight Luau module for Roblox that wraps <code>DataStoreService</code> with a session-safe, path-based API.</p> <p>AI-Assisted Documentation</p> <p>Documentation is AI-Assisted and may contain inaccuracies. Please report any issues or suggestions on the GitHub Issues Page.</p>"},{"location":"#features","title":"Features","text":"Feature Description Session Locking Prevents concurrent server writes to the same player key. Auto-Save Saves all active souls in the background at a configurable interval. Path-Based API Navigate nested data with key arrays instead of raw table references. Change Listeners Subscribe to value changes at any path depth. Lifecycle Hooks Transform or validate data before save and after load. Graceful Shutdown Flushes all souls before the server closes via <code>BindToClose</code>."},{"location":"#quick-start","title":"Quick Start","text":"<p><pre><code>rojo build -o SoulStore.rbxlx\n</code></pre> Place <code>SoulStore.lua</code> in <code>ServerScriptService</code> or a shared server <code>Modules</code> folder and <code>require</code> it from a <code>Script</code>. <pre><code>local SoulStore = require(path.to.SoulStore)\n\nlocal DEFAULT_DATA = { Coins = 0, Level = 1 }\n\ngame.Players.PlayerAdded:Connect(function(player)\n    local soul = SoulStore.new(\"PlayerData\", player, DEFAULT_DATA)\n    soul:LoadData()\n    soul:SetData({ \"Coins\" }, soul:GetData({ \"Coins\" }) + 50)\nend)\n</code></pre></p> <p>Server-Only</p> <p><code>DataStoreService</code> is unavailable on the client. Never require SoulStore from a <code>LocalScript</code>.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<p>You may install SoulStore in one of two ways:</p>"},{"location":"getting-started/#rojo","title":"Rojo","text":"<ol> <li>Git clone or download the repository onto your computer</li> <li>Run <code>rojo build --o SoulStore.rbxlx</code></li> <li>Copy 'SoulStore' in ServerScriptService into your desired location</li> </ol>"},{"location":"getting-started/#wally","title":"Wally","text":"<ol> <li>Edit your <code>wally.toml</code> file to include the following: <pre><code>[dependencies]\nSoulStore = \"mystifine/soulstore@1.0.3\"\n</code></pre></li> <li>Run <code>wally install</code></li> </ol>"},{"location":"getting-started/#importing-soulstore","title":"Importing SoulStore","text":"<p>In your scripts, require the module: <pre><code>local SoulStore = require(path.to.SoulStore)\n</code></pre></p>"},{"location":"getting-started/#common-patterns","title":"Common Patterns","text":"<p>SoulStore is designed to be used in a common pattern. It is recommended to use the following pattern to ensure data integrity and prevent data loss.</p>"},{"location":"getting-started/#data-schema","title":"Data Schema","text":"<p>Create a table of default data to be used as a schema. <pre><code>local PlayerDataSchema = {\n    Stats = {\n        Gold = 0,\n        Gems = 0,\n    },\n    Units = {},\n    Bag = {},\n    Equipments = {},\n}\n\nreturn PlayerDataSchema\n</code></pre></p>"},{"location":"getting-started/#creating-a-playerdataservice","title":"Creating a PlayerDataService","text":"<p>Create a service that handles player data. This service will be responsible for creating and managing souls. <pre><code>local ServerScriptService = game.ServerScriptService;\nlocal Players = game.Players;\nlocal ReplicatedStorage = game.ReplicatedStorage;\n\nlocal SoulStore = require(ServerScriptService.Dependencies.SoulStore);\n\nlocal PlayerDataSchema = require(ServerScriptService.Constants.PlayerDataSchema); -- A table of default data\n\nlocal playerSouls : {[Player] : SoulStore.Soul} = {};\nlocal PlayerDataService = {}\n\nfunction PlayerDataService._initPlayerData(player : Player)\n    local newSoul = SoulStore.new(\"PlayerData\", player, PlayerDataSchema);\n    newSoul:LoadData();\n    newSoul:Reconcile(PlayerDataSchema);\n\n    -- This is an example of how to create a container for stats and attach change listeners to update the data through instance value property change\n    local statsContainer = Instance.new(\"Folder\")\n    statsContainer.Name = \"Stats\";\n    statsContainer.Parent = player;\n\n    local DATA_TYPE_TO_OBJECT = {\n        [\"string\"] = \"StringValue\",\n        [\"number\"] = \"NumberValue\",\n        [\"boolean\"] = \"BoolValue\"\n    }\n    local playerStats = newSoul:GetData({\"Stats\"});\n    for statName, statValue in pairs(playerStats) do\n        local statObject = Instance.new(DATA_TYPE_TO_OBJECT[typeof(statValue)]);\n        statObject.Name = statName;\n        statObject.Value = statValue;\n        statObject.Parent = statsContainer;\n\n        newSoul:OnDataChanged({\"Stats\", statName}, function(oldData: any?, newData: any?): nil \n            statObject.Value = newData;\n        end)\n    end\n\n    local dataLoaded = Instance.new(\"BoolValue\");\n    dataLoaded.Value = true;\n    dataLoaded.Name = \"dataLoaded\";\n    dataLoaded.Parent = player;\n\n    playerSouls[player] = newSoul;\nend\n\nfunction PlayerDataService._onPlayerRemoving(player : Player)\n    local soul = playerSouls[player];\n    if soul then\n        soul:SaveData(true);\n    end\n    playerSouls[player] = nil;\nend\n\nfunction PlayerDataService.getSoul(player : Player)\n    local dataLoaded = PlayerDataService.waitForDataLoaded(player);\n    return playerSouls[player];\nend\n\nfunction PlayerDataService.main()\n    Players.PlayerAdded:Connect(PlayerDataService._initPlayerData);\n    local players = Players:GetPlayers();\n    for i = 1, #players do\n        task.spawn(PlayerDataService._initPlayerData, players[i]);\n    end\n\n    Players.PlayerRemoving:Connect(PlayerDataService._onPlayerRemoving);\nend\n\nreturn PlayerDataService\n</code></pre></p>"},{"location":"getting-started/#configuration","title":"Configuration","text":"<p>Adjust the <code>SOUL_STORE_SETTINGS</code> table at the top of the module file before deploying.</p>"},{"location":"getting-started/#debug_mode","title":"<code>DEBUG_MODE</code>","text":"Type <code>boolean</code> Default <code>true</code> <p>Enables console logging for load/save events, session-lock warnings, and errors. Disable in production.</p> <pre><code>DEBUG_MODE = false\n</code></pre>"},{"location":"getting-started/#trace_back_message","title":"<code>TRACE_BACK_MESSAGE</code>","text":"Type <code>boolean</code> Default <code>false</code> <p>When <code>true</code>, appends a full <code>debug.traceback()</code> string to every debug log. Useful during development to trace the origin of unexpected save/load calls.</p>"},{"location":"getting-started/#minimum_save_interval","title":"<code>MINIMUM_SAVE_INTERVAL</code>","text":"Type <code>number</code> (seconds) Default <code>6</code> <p>Minimum delay between retry attempts when a save operation fails. Prevents hammering the DataStore API on repeated errors.</p>"},{"location":"getting-started/#minimum_load_interval","title":"<code>MINIMUM_LOAD_INTERVAL</code>","text":"Type <code>number</code> (seconds) Default <code>6</code> <p>Minimum delay between retry attempts when a load fails or a key is session-locked by another server.</p>"},{"location":"getting-started/#auto_save_interval","title":"<code>AUTO_SAVE_INTERVAL</code>","text":"Type <code>number</code> (seconds) Default <code>30</code> <p>How often SoulStore auto-saves all active souls in the background. The internal loop ticks every 10 seconds and saves any soul whose <code>LastAutoSave</code> has exceeded this value.</p> <p>Tip</p> <p>Values between 30\u2013120 seconds are recommended. Lower values consume more DataStore budget; higher values increase potential data loss on a crash.</p>"},{"location":"getting-started/#session_lock_auto_release","title":"<code>SESSION_LOCK_AUTO_RELEASE</code>","text":"Type <code>number</code> (seconds) Default <code>300</code> (5 min) <p>How long another server waits before forcibly claiming a session-locked key after the original server appears to have crashed.</p> <p>Safe Tuning Rule</p> <p>Always set <code>SESSION_LOCK_AUTO_RELEASE</code> to at least 3\u00d7 your <code>AUTO_SAVE_INTERVAL</code>. If the release window is shorter than the save interval, a slow auto-save could allow another server to steal the lock mid-session, causing a data conflict.</p>"},{"location":"api/soul/","title":"Soul Methods","text":"<p>Instance methods available on a <code>Soul</code> object.</p>"},{"location":"api/soul/#loaddata","title":"<code>:LoadData()</code>","text":"<p>Loads player data from the DataStore and acquires a session lock.</p> <p>Signature <pre><code>soul:LoadData() -&gt; nil\n</code></pre></p> <p>Behaviour</p> <ul> <li>Yields the calling thread until data loads or the player leaves.</li> <li>Retries indefinitely while the key is session-locked or a DataStore error occurs.</li> <li>Writes default data if no existing save is found.</li> <li>Fires the <code>SetOnLoad</code> callback before <code>LoadState</code> transitions to <code>\"Loaded\"</code>.</li> <li>Caches the soul internally once loading succeeds.</li> </ul> <p>State Guards</p> Precondition Result <code>LoadState == \"Loaded\"</code> No-op <code>LoadState == \"Loading\"</code> No-op Player not in game No-op <p>Warning</p> <p><code>:LoadData()</code> yields. Wrap in <code>task.spawn</code> if you need non-blocking startup.</p> <p>Example <pre><code>game.Players.PlayerAdded:Connect(function(player)\n    local soul = SoulStore.new(\"PlayerData\", player, DEFAULT_DATA)\n    soul:LoadData()\n\n    if soul.LoadState == \"Loaded\" then\n        print(\"Coins:\", soul:GetData({ \"Coins\" }))\n    end\nend)\n</code></pre></p>"},{"location":"api/soul/#savedatasessionending","title":"<code>:SaveData(sessionEnding?)</code>","text":"<p>Saves the soul's current data to the DataStore.</p> <p>Signature <pre><code>soul:SaveData(sessionEnding: boolean?) -&gt; nil\n</code></pre></p> <p>Parameters</p> Parameter Type Default Description <code>sessionEnding</code> <code>boolean?</code> <code>false</code> When <code>true</code>, releases the session lock and cleans up the soul. <p>Behaviour</p> <ul> <li>Mid-session save: increments <code>SaveId</code>, updates <code>LastUpdate</code>, keeps the lock.</li> <li>Session-ending save: sets <code>Locked = false</code> and <code>SessionId = \"\"</code>, fires <code>SessionEndBindableEvent</code>, and removes the soul from cache.</li> <li>If a session-ending save is triggered while a regular save is running, the regular save is interrupted.</li> <li>Retries on DataStore failure until success or the save state changes.</li> </ul> <p>State Guards</p> Precondition Result <code>LoadState ~= \"Loaded\"</code> No-op <code>SaveState == \"SessionEnding\"</code> No-op <code>SaveState == \"Saving\"</code> and not session-ending No-op <p>Example <pre><code>soul:SaveData()        -- mid-session manual save (e.g. after a purchase)\nsoul:SaveData(true)    -- session-ending (called automatically on PlayerRemoving)\n</code></pre></p>"},{"location":"api/soul/#getdatapath","title":"<code>:GetData(path?)</code>","text":"<p>Retrieves a value at the given path from <code>soul.Data</code>.</p> <p>Signature <pre><code>soul:GetData(path: {any}?) -&gt; any?\n</code></pre></p> <p>Parameters</p> Parameter Type Description <code>path</code> <code>{any}?</code> Array of keys to traverse. Pass <code>nil</code> to return the full data table. <p>Returns The value at the path, or <code>nil</code> if any key is missing or data is not loaded.</p> <p>Example <pre><code>local all   = soul:GetData()\nlocal coins = soul:GetData({ \"Coins\" })\nlocal level = soul:GetData({ \"Stats\", \"Level\" })\nlocal item  = soul:GetData({ \"Inventory\", 1 })\n</code></pre></p>"},{"location":"api/soul/#setdatapath-value","title":"<code>:SetData(path, value)</code>","text":"<p>Sets a value at the given path in <code>soul.Data</code> and fires any registered change listeners.</p> <p>Signature <pre><code>soul:SetData(path: {any}, value: any) -&gt; any?\n</code></pre></p> <p>Parameters</p> Parameter Type Description <code>path</code> <code>{any}</code> One or more keys. Must not be empty. <code>value</code> <code>any</code> The value to assign at the final key. <p>Returns The value that was set, or <code>nil</code> on failure.</p> <p>Intermediate Keys Must Exist</p> <p><code>:SetData()</code> traverses but does not create intermediate tables. Ensure all nested structures are pre-defined in <code>DEFAULT_DATA</code>.</p> <p>Example <pre><code>soul:SetData({ \"Coins\" }, 500)\nsoul:SetData({ \"Stats\", \"Level\" }, 10)\nsoul:SetData({ \"Inventory\" }, { \"Sword\", \"Shield\" })\n</code></pre></p>"},{"location":"api/soul/#reconciledata","title":"<code>:Reconcile(data)</code>","text":"<p>Merges an external table into <code>soul.Data</code>, filling in only keys that are currently <code>nil</code>.</p> <p>Signature <pre><code>soul:Reconcile(data: {}?) -&gt; nil\n</code></pre></p> <p>Behaviour</p> <ul> <li>Recursive \u2014 handles nested tables.</li> <li>Never overwrites an existing value.</li> <li>Warns and returns early if data is not loaded or <code>data</code> is <code>nil</code>.</li> </ul> <p>Example <pre><code>local DEFAULTS = { Coins = 0, Level = 1, NewField = \"hello\" }\n\nsoul:SetOnLoad(function()\n    soul:Reconcile(DEFAULTS) -- back-fills NewField on old saves\nend)\n</code></pre></p>"},{"location":"api/soul/#ondatachangedpath-callback","title":"<code>:OnDataChanged(path, callback)</code>","text":"<p>Registers a listener that fires when a value at <code>path</code> changes via <code>:SetData()</code>.</p> <p>Signature <pre><code>soul:OnDataChanged(\n    path     : {string | number},\n    callback : (oldData: any?, newData: any?) -&gt; nil\n) -&gt; { Disconnect: () -&gt; nil }\n</code></pre></p> <p>Parameters</p> Parameter Type Description <code>path</code> <code>{string | number}</code> Path to watch. Also fires for <code>:SetData()</code> calls whose path passes through this path. <code>callback</code> <code>function</code> Receives <code>oldData</code> and <code>newData</code>. Called via <code>task.spawn</code>. <p>Returns A connection object with a <code>Disconnect()</code> method.</p> <p>Partial Path Matching</p> <p>A listener on <code>{ \"Stats\" }</code> fires when <code>:SetData({ \"Stats\", \"Level\" }, 5)</code> is called. See the Change Listeners guide for details.</p> <p>Example <pre><code>local conn = soul:OnDataChanged({ \"Coins\" }, function(old, new)\n    print((\"Coins: %d \u2192 %d\"):format(old, new))\nend)\nsoul:SetData({ \"Coins\" }, 100) --&gt; Coins: 0 \u2192 100\nconn:Disconnect()\n</code></pre></p>"},{"location":"api/soul/#setonloadcallback","title":"<code>:SetOnLoad(callback)</code>","text":"<p>Registers a callback called after data is read from the DataStore, before <code>LoadState</code> becomes <code>\"Loaded\"</code>.</p> <p>Signature <pre><code>soul:SetOnLoad(callback: (data: {}) -&gt; nil) -&gt; nil\n</code></pre></p> <p>The callback receives the raw data table by reference. Mutations affect what the soul stores in memory.</p> <p>Note</p> <p>Must be called before <code>:LoadData()</code>.</p> <p>Example <pre><code>soul:SetOnLoad(function(data)\n    data.Coins = math.clamp(data.Coins or 0, 0, 1_000_000)\nend)\nsoul:LoadData()\n</code></pre></p>"},{"location":"api/soul/#setonsavecallback","title":"<code>:SetOnSave(callback)</code>","text":"<p>Registers a callback called before each DataStore write, receiving a deep copy of <code>soul.Data</code>.</p> <p>Signature <pre><code>soul:SetOnSave(callback: (data: {}) -&gt; nil) -&gt; nil\n</code></pre></p> <p>Mutations affect only the save snapshot \u2014 <code>soul.Data</code> is not changed.</p> <p>Example <pre><code>soul:SetOnSave(function(snapshot)\n    snapshot.TempBoost = nil -- strip runtime-only field before saving\nend)\n</code></pre></p>"},{"location":"api/soulstore/","title":"SoulStore Static Methods","text":"<p>Static methods on the <code>SoulStore</code> module table.</p>"},{"location":"api/soulstore/#soulstorenew","title":"<code>SoulStore.new()</code>","text":"<p>Creates a new <code>Soul</code> object. Returns an existing cached soul if one is already loaded for this player and DataStore ID.</p> <p>Signature <pre><code>SoulStore.new(\n    datastoreId : string,\n    player      : Player,\n    defaultData : {}\n) -&gt; Soul?\n</code></pre></p> <p>Parameters</p> Parameter Type Description <code>datastoreId</code> <code>string</code> DataStore name to read from and write to. <code>player</code> <code>Player</code> The <code>Player</code> instance. <code>defaultData</code> <code>{}</code> Starting data template. Deep-copied into the soul. <p>Returns <code>Soul?</code> \u2014 <code>nil</code> if <code>defaultData</code> is not a table or the player has left.</p> <pre><code>local soul = SoulStore.new(\"PlayerData\", player, { Coins = 0, Level = 1 })\n</code></pre>"},{"location":"api/soulstore/#soulstoregetsoul","title":"<code>SoulStore.getSoul()</code>","text":"<p>Retrieves a cached soul by DataStore ID and player.</p> <p>Signature <pre><code>SoulStore.getSoul(datastoreId: string, player: Player) -&gt; Soul?\n</code></pre></p> <p>Returns <code>Soul?</code> \u2014 the cached soul, or <code>nil</code> if not found.</p> <pre><code>local soul = SoulStore.getSoul(\"PlayerData\", player)\nif soul then\n    soul:SetData({ \"Coins\" }, 999)\nend\n</code></pre>"},{"location":"api/soulstore/#soulstoreplayerexists","title":"<code>SoulStore.playerExists()</code>","text":"<p>Returns <code>true</code> if the player is currently in the game.</p> <p>Signature <pre><code>SoulStore.playerExists(player: Player?) -&gt; boolean?\n</code></pre></p> <p>Checks <code>player and player:IsA(\"Player\") and player.Parent == Players</code>.</p>"},{"location":"api/soulstore/#soulstoregetmetadata","title":"<code>SoulStore.getMetaData()</code>","text":"<p>Returns a fresh <code>SoulMetaData</code> table for the current server and timestamp. Called internally during first-time data creation.</p> <p>Signature <pre><code>SoulStore.getMetaData() -&gt; SoulMetaData\n-- Returns: { Locked = true, SaveId = 0, LastUpdate = os.time(), SessionId = game.JobId }\n</code></pre></p>"},{"location":"api/soulstore/#soulstoredeepcopy","title":"<code>SoulStore.deepCopy()</code>","text":"<p>Recursively copies a table, or returns a primitive value unchanged.</p> <p>Signature <pre><code>SoulStore.deepCopy(data: any?) -&gt; any\n</code></pre></p> <pre><code>local copy = SoulStore.deepCopy({ Stats = { Level = 5 } })\ncopy.Stats.Level = 99\n-- original table is unchanged\n</code></pre>"},{"location":"api/soulstore/#soulstoreoutput","title":"<code>SoulStore.output()</code>","text":"<p>Logs a message using the provided function, respecting <code>DEBUG_MODE</code> and <code>TRACE_BACK_MESSAGE</code>. No-ops if <code>DEBUG_MODE</code> is <code>false</code>.</p> <p>Signature <pre><code>SoulStore.output(outputFunction: (any) -&gt; any, ...: any) -&gt; nil\n</code></pre></p>"},{"location":"api/soulstore/#soulstoreresetdata","title":"<code>SoulStore.resetData()</code>","text":"<p>Removes a single player's DataStore key.</p> <p>Signature <pre><code>SoulStore.resetData(datastoreId: string, datastoreKey: string) -&gt; nil\n</code></pre></p> <p>Data Loss</p> <p>Permanently deletes the player's saved data. Cannot be undone.</p> <pre><code>SoulStore.resetData(\"PlayerData\", tostring(player.UserId))\n</code></pre>"},{"location":"api/soulstore/#soulstoreresetalldata","title":"<code>SoulStore.resetAllData()</code>","text":"<p>Deletes every key in a DataStore. Waits 10 seconds, then pages through all keys and removes each one sequentially.</p> <p>Signature <pre><code>SoulStore.resetAllData(datastoreId: string) -&gt; nil\n</code></pre></p> <p>Irreversible \u2014 Extreme Caution</p> <p>Permanently wipes all player data. See the Resetting Data guide before using.</p>"},{"location":"api/types/","title":"Types","text":"<p>SoulStore exports three Luau types for use in typed scripts.</p>"},{"location":"api/types/#soulmetadata","title":"<code>SoulMetaData</code>","text":"<p>Internal metadata stored alongside every player's data table. Managed automatically \u2014 do not write to these fields manually.</p> <pre><code>export type SoulMetaData = {\n    Locked     : boolean, -- true while a server owns the session\n    SaveId     : number,  -- incremented on every successful write\n    LastUpdate : number,  -- os.time() of the last DataStore write\n    SessionId  : string,  -- game.JobId of the owning server; \"\" when unlocked\n}\n</code></pre> Field Type Description <code>Locked</code> <code>boolean</code> Whether the key is owned by an active server. <code>SaveId</code> <code>number</code> Monotonic save counter. Useful for auditing. <code>LastUpdate</code> <code>number</code> Timestamp of the last write; used to calculate lock expiry. <code>SessionId</code> <code>string</code> <code>game.JobId</code> of the owner. Empty string when unlocked. <p>Note</p> <p><code>MetaData</code> lives at <code>soul.Data.MetaData</code> and is persisted alongside player data.</p>"},{"location":"api/types/#soul","title":"<code>Soul</code>","text":"<p>Represents a single player's live data session.</p> <pre><code>export type Soul = {\n    DatastoreId             : string,\n    Player                  : Player,\n    Data                    : { MetaData : SoulMetaData },\n    LastAutoSave            : number,\n    LoadState               : string, -- \"Idle\" | \"Loading\" | \"Loaded\"\n    SaveState               : string, -- \"Idle\" | \"Saving\" | \"SessionEnding\"\n    SessionEndBindableEvent : BindableEvent,\n}\n</code></pre> Property Type Description <code>DatastoreId</code> <code>string</code> DataStore name this soul reads/writes. <code>Player</code> <code>Player</code> The associated <code>Player</code> instance. <code>Data</code> <code>table</code> Live data table. Mutated by <code>:SetData()</code>. <code>LastAutoSave</code> <code>number</code> Timestamp of the last auto-save. <code>LoadState</code> <code>string</code> Current load phase. <code>SaveState</code> <code>string</code> Current save phase. <code>SessionEndBindableEvent</code> <code>BindableEvent</code> Fires once after a successful session-ending save."},{"location":"api/types/#soulstore","title":"<code>SoulStore</code>","text":"<p>The module table, usable as a static namespace.</p> <pre><code>export type SoulStore = {\n    ClassName    : string,\n    new          : (datastoreId: string, player: Player, defaultData: {}) -&gt; Soul?,\n    getSoul      : (datastoreId: string, player: Player) -&gt; Soul?,\n    playerExists : (player: Player?) -&gt; boolean?,\n    getMetaData  : () -&gt; SoulMetaData,\n    deepCopy     : (data: any?) -&gt; any,\n    output       : (fn: (any) -&gt; any, ...any) -&gt; nil,\n    resetData    : (datastoreId: string, datastoreKey: string) -&gt; nil,\n    resetAllData : (datastoreId: string) -&gt; nil,\n}\n</code></pre>"},{"location":"guides/data-operations/","title":"Data Operations","text":""},{"location":"guides/data-operations/#path-arrays","title":"Path Arrays","text":"<p>All data access uses path arrays \u2014 ordered lists of keys describing how to traverse <code>soul.Data</code>.</p> <pre><code>-- Given:\n-- { Stats = { Level = 5, XP = 120 }, Inventory = { \"Sword\", \"Shield\" } }\n\nsoul:GetData({ \"Stats\", \"Level\" }) --&gt; 5\nsoul:GetData({ \"Stats\" })          --&gt; { Level = 5, XP = 120 }\nsoul:GetData({ \"Inventory\", 1 })   --&gt; \"Sword\"\nsoul:GetData()                     --&gt; full data table\n</code></pre>"},{"location":"guides/data-operations/#reading-data","title":"Reading Data","text":"<pre><code>local coins = soul:GetData({ \"Coins\" })\nlocal level = soul:GetData({ \"Stats\", \"Level\" })\nlocal all   = soul:GetData()\n</code></pre> <p>Returns <code>nil</code> and logs a warning if any key in the path doesn't exist.</p>"},{"location":"guides/data-operations/#writing-data","title":"Writing Data","text":"<pre><code>soul:SetData({ \"Coins\" }, 500)\nsoul:SetData({ \"Stats\", \"Level\" }, level + 1)\nsoul:SetData({ \"Inventory\" }, { \"Sword\", \"Shield\", \"Potion\" })\n</code></pre> <p>Intermediate Keys Must Exist</p> <p><code>:SetData()</code> traverses but does not create intermediate tables. Define all nested structures in <code>DEFAULT_DATA</code>.</p>"},{"location":"guides/data-operations/#mutating-sub-tables","title":"Mutating Sub-Tables","text":"<p><code>:GetData()</code> returns a reference to the live table. Mutate and write back to trigger listeners:</p> <pre><code>local inv = soul:GetData({ \"Inventory\" })\ntable.insert(inv, \"New Item\")\nsoul:SetData({ \"Inventory\" }, inv) -- triggers listeners\n</code></pre>"},{"location":"guides/data-operations/#reconcile","title":"Reconcile","text":"<p>Merges a source table into <code>soul.Data</code>, adding only keys that are missing:</p> <pre><code>local DEFAULTS = {\n    Coins           = 0,\n    Level           = 1,\n    PremiumCurrency = 0, -- newly added field\n}\n\nsoul:SetOnLoad(function()\n    soul:Reconcile(DEFAULTS) -- adds PremiumCurrency to old saves only\nend)\n</code></pre>"},{"location":"guides/data-operations/#checking-load-state","title":"Checking Load State","text":"<p>Verify <code>soul.LoadState</code> before operating on data outside of the initial <code>PlayerAdded</code> flow:</p> <pre><code>if soul.LoadState == \"Loaded\" then\n    soul:SetData({ \"Coins\" }, soul:GetData({ \"Coins\" }) + 10)\nend\n</code></pre>"},{"location":"guides/lifecycle-hooks/","title":"Lifecycle Hooks","text":""},{"location":"guides/lifecycle-hooks/#setonload","title":"SetOnLoad","text":"<p>Called after data is read from the DataStore, before <code>LoadState</code> becomes <code>\"Loaded\"</code>. The callback receives the raw data table by reference \u2014 mutations affect what the soul stores in memory.</p> <p>Common use cases: reconcile new fields, clamp/validate values, migrate old data schemas.</p> <pre><code>soul:SetOnLoad(function(data)\n    -- Back-fill new fields\n    if data.PremiumCurrency == nil then\n        data.PremiumCurrency = 0\n    end\n\n    -- Clamp coin balance\n    data.Coins = math.clamp(data.Coins or 0, 0, 1_000_000)\n\n    -- Schema migration example\n    if data.OldXP ~= nil then\n        data.Stats = data.Stats or {}\n        data.Stats.XP = data.OldXP\n        data.OldXP = nil\n    end\nend)\n\nsoul:LoadData() -- must be called AFTER SetOnLoad\n</code></pre> <p>Note</p> <p>Call <code>:SetOnLoad()</code> before <code>:LoadData()</code>.</p>"},{"location":"guides/lifecycle-hooks/#setonsave","title":"SetOnSave","text":"<p>Called before each DataStore write. Receives a deep copy of <code>soul.Data</code>. Mutations affect only the snapshot \u2014 the live in-memory data is unchanged.</p> <p>Common use cases: strip runtime fields, save computed aggregates, redact fields before storage.</p> <pre><code>soul:SetOnSave(function(snapshot)\n    -- Strip fields only valid this session\n    snapshot.ActiveBoostExpiry = nil\n    snapshot.TempEventData     = nil\n\n    -- Save aggregate playtime\n    snapshot.TotalPlaytime = (snapshot.TotalPlaytime or 0)\n        + (os.time() - sessionStartTime)\nend)\n</code></pre> <p>Warning</p> <p>Mutations to the snapshot do not affect <code>soul.Data</code>. Update live data separately via <code>:SetData()</code> if needed.</p>"},{"location":"guides/lifecycle-hooks/#execution-order","title":"Execution Order","text":"<pre><code>DataStore read\n    \u2514\u2500\u2500 SetOnLoad  (mutates live data in place)\n        \u2514\u2500\u2500 LoadState = \"Loaded\"\n            \u2514\u2500\u2500 ... game logic ...\n                \u2514\u2500\u2500 SaveData triggered\n                    \u2514\u2500\u2500 deep copy of soul.Data created\n                        \u2514\u2500\u2500 SetOnSave  (mutates snapshot only)\n                            \u2514\u2500\u2500 DataStore write\n</code></pre>"},{"location":"guides/listeners/","title":"Change Listeners","text":"<p><code>:OnDataChanged()</code> lets you react to mutations made through <code>:SetData()</code>.</p>"},{"location":"guides/listeners/#basic-usage","title":"Basic Usage","text":"<pre><code>local conn = soul:OnDataChanged({ \"Coins\" }, function(oldValue, newValue)\n    print((\"Coins: %d \u2192 %d\"):format(oldValue, newValue))\nend)\n\nsoul:SetData({ \"Coins\" }, 100)\n-- Output: Coins: 0 \u2192 100\n</code></pre>"},{"location":"guides/listeners/#disconnecting","title":"Disconnecting","text":"<pre><code>local conn = soul:OnDataChanged({ \"Coins\" }, function(old, new) end)\nconn:Disconnect()\n</code></pre>"},{"location":"guides/listeners/#partial-path-matching","title":"Partial Path Matching","text":"<p>Listeners fire for every path segment a <code>:SetData()</code> call passes through:</p> <pre><code>soul:OnDataChanged({ \"Stats\" }, function(old, new)\n    -- Fires when any key under \"Stats\" changes.\n    -- old/new are the entire Stats sub-table.\nend)\n\nsoul:OnDataChanged({ \"Stats\", \"Level\" }, function(old, new)\n    -- Fires only for the exact \"Stats.Level\" key.\nend)\n\nsoul:SetData({ \"Stats\", \"Level\" }, 10)\n-- Both listeners above fire.\n</code></pre>"},{"location":"guides/listeners/#practical-patterns","title":"Practical Patterns","text":"<p>Track inventory size: <pre><code>soul:OnDataChanged({ \"Inventory\" }, function(_, newInv)\n    print(\"Inventory size:\", #newInv)\nend)\n</code></pre></p> <p>React to any stat change: <pre><code>soul:OnDataChanged({ \"Stats\" }, function(_, newStats)\n    updateLeaderboard(player, newStats)\nend)\n</code></pre></p> <p>Separate Threads</p> <p>Listeners are called via <code>task.spawn</code> and run concurrently. They never block <code>:SetData()</code>.</p>"},{"location":"guides/reset-data/","title":"Resetting Data","text":"<p>Danger</p> <p>Both reset functions permanently delete DataStore data. There is no undo.</p>"},{"location":"guides/reset-data/#reset-a-single-player","title":"Reset a Single Player","text":"<pre><code>SoulStore.resetData(\"PlayerData\", tostring(player.UserId))\n</code></pre> <p>Calls <code>DataStoreService:RemoveAsync</code> on the given key. Retries once and warns on failure.</p> <p>When to use:</p> <ul> <li>Clearing a QA tester's data between test runs.</li> <li>Admin command to reset a specific player's progress by request.</li> <li>Removing a known corrupted key.</li> </ul>"},{"location":"guides/reset-data/#reset-an-entire-datastore","title":"Reset an Entire DataStore","text":"<pre><code>SoulStore.resetAllData(\"PlayerData\")\n</code></pre> <p>This function:</p> <ol> <li>Logs a warning block and waits 10 seconds before starting.</li> <li>Pages through all keys using <code>ListKeysAsync</code>.</li> <li>Calls <code>RemoveAsync</code> on each key sequentially, retrying on failure.</li> <li>Logs progress per key and a completion message.</li> </ol> <p>Requirements:</p> <ul> <li>Run in Roblox Studio only \u2014 never on a live server.</li> <li>No active player sessions in the target DataStore.</li> <li>Do not close Studio or interrupt the script during the operation.</li> </ul> <p>No Cancel Mechanism</p> <p>The only safety net is the 10-second delay. Once the loop begins it cannot be stopped from within the script.</p>"},{"location":"guides/reset-data/#safe-usage-pattern","title":"Safe Usage Pattern","text":"<p>Guard all reset calls behind a Studio-only check:</p> <pre><code>local RunService = game:GetService(\"RunService\")\n\nif RunService:IsStudio() then\n    -- Uncomment exactly one:\n    -- SoulStore.resetData(\"PlayerData\", \"123456789\")\n    -- SoulStore.resetAllData(\"PlayerData\")\nend\n</code></pre> <p>Run via the Studio command bar or a <code>Script</code> with <code>RunContext = Legacy</code>.</p>"},{"location":"guides/session-locking/","title":"Session Locking","text":""},{"location":"guides/session-locking/#the-problem","title":"The Problem","text":"<p>Roblox does not guarantee only one server has a player loaded at a time. During teleports, crashes, or shutdown races, two servers can briefly both believe they own the same player. If both write to the same DataStore key, the last write wins \u2014 potentially overwriting progress.</p>"},{"location":"guides/session-locking/#how-soulstore-handles-it","title":"How SoulStore Handles It","text":"<p>On load, SoulStore uses <code>UpdateAsync</code> to atomically claim ownership of the key:</p> <pre><code>readData.MetaData.Locked     = true\nreadData.MetaData.SessionId  = game.JobId\nreadData.MetaData.LastUpdate = os.time()\n</code></pre> <p>A second server seeing <code>Locked = true</code> with a foreign <code>SessionId</code> enters a retry loop and logs the time remaining until the lock expires. It cannot write to the key until it acquires the lock.</p> <p>On a clean session end, the lock is released:</p> <pre><code>MetaData.Locked    = false\nMetaData.SessionId = \"\"\n</code></pre>"},{"location":"guides/session-locking/#lock-expiry-crash-recovery","title":"Lock Expiry (Crash Recovery)","text":"<p>If a server crashes without releasing its lock, the lock expires after <code>SESSION_LOCK_AUTO_RELEASE</code> seconds. The waiting server calculates:</p> <pre><code>timeRemaining = SESSION_LOCK_AUTO_RELEASE - (os.time() - MetaData.LastUpdate)\n</code></pre> <p>Because <code>LastUpdate</code> is refreshed on every auto-save, frequent saves reduce how long a second server must wait after a crash.</p>"},{"location":"guides/session-locking/#tuning-recommendations","title":"Tuning Recommendations","text":"Use Case <code>AUTO_SAVE_INTERVAL</code> <code>SESSION_LOCK_AUTO_RELEASE</code> Casual game 60s 120s RPG / economy 60s 300s Competitive / ranked 60s 600s <p>Warning</p> <p>Always keep <code>SESSION_LOCK_AUTO_RELEASE \u2265 AUTO_SAVE_INTERVAL \u00d7 3</code>. If the release window is shorter than the save interval, a slow auto-save can allow another server to steal the lock mid-session.</p>"},{"location":"guides/session-locking/#graceful-shutdown","title":"Graceful Shutdown","text":"<p>SoulStore binds to <code>game:BindToClose</code>, which Roblox calls before planned server shutdowns:</p> <ol> <li>Spawns a session-ending save for every cached soul.</li> <li>Yields until all <code>SessionEndBindableEvent</code> signals fire.</li> <li>All locks are released before the process exits.</li> </ol> <p>Crash scenarios rely on the lock expiry mechanism above.</p>"}]}